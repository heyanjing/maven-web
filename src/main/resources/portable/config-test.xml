<?xml version="1.0" encoding="utf-8" ?>
<!-- 测试环境配置 -->
<!-- 注意：当替换的内容出现XML特殊字符时，请使用<![CDATA[...]]> -->
<!-- 注意：值内容尽量不要换行 -->
<portable-config>
    <config-file path="WEB-INF/classes/config/config.properties">

        <!-- APPLICATION -->
        <replace key="APP_DOMAIN">http://test.jsyxx.cn</replace>
        <replace key="APP_DEBUG">false</replace>

        <!-- OSS Or Local -->
        <replace key="DIR_URL">http://data-test.timingbar.com/${APP_NAME}</replace>

        <!-- JDBC -->
        <replace key="APP_DATABASE_HBM2DDL_AUTO">none</replace>
        <replace key="APP_DATABASE_DRIVER"><![CDATA[com.mysql.jdbc.Driver]]></replace>
        <replace key="APP_DATABASE_URL"><![CDATA[jdbc:mysql://rdsj6z92hf17l111k2l9public.mysql.rds.aliyuncs.com:3306/edu_test?useUnicode=true&characterEncoding=utf-8&autoReconnect=true&failOverReadOnly=false]]></replace>
        <replace key="APP_DATABASE_USERNAME">tb_edu</replace>
        <replace key="APP_DATABASE_PASSWORD">tb_edu123</replace>

        <!-- STATIC -->
        <replace key="STATIC_URL">http://static.timingbar.com</replace>
        <replace key="STATIC_CTX_URL">http://static-edu.timingbar.com</replace>

        <!-- API -->
        <replace key="API_URL">http://api.timingbar.com</replace>
        <replace key="API_APP">timingbar.edutest</replace>
        <replace key="API_KEY">tbadmin\,.</replace>

        <!-- MAIL -->
        <replace key="MAIL_HOST">smtp.mxhichina.com</replace>
        <replace key="MAIL_USER">service@jsyxx.cn</replace>
        <replace key="MAIL_PASS">Tbadmin123456</replace>

    </config-file>
</portable-config>